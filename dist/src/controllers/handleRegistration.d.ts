import type { RegistrationSocketMessage } from '../lib/types.ts';
import { type ISocketChannel } from '../helpers/index.ts';
export declare const handleRegistration: ({ message, socketChannel, }: {
    message: RegistrationSocketMessage;
    socketChannel: ISocketChannel;
}) => Promise<void>;
//# sourceMappingURL=handleRegistration.d.ts.map